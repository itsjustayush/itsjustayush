{
  "name": "itsjustayush",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio website of Ayush Bhattacharya — static single-page site built with HTML, CDN Tailwind, and vanilla JS.",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server ${PORT:-8080} --bind 0.0.0.0",
    "preview": "npm run dev",
    "build": "rm -rf dist && mkdir -p dist && cp index.html favicon.png dist/ && cp -r images guestbook dist/"
  }
}
